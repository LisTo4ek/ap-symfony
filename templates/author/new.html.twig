{% extends 'base.html.twig' %}

{% block title %}Create Author{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create Author</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary mt-3">Create</button>
            <a href="{{ path('app_author_index') }}" class="btn btn-secondary mt-3">Cancel</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
